# Coverage config for GitHub / `make test-ci` (no ML extra on the runner).
# Local `make test` uses pyproject.toml (app/ml except vendor).

[run]
source = app
omit =
    app/ml/*

[report]
fail_under = 60
show_missing = true
exclude_lines =
    pragma: no cover
    if TYPE_CHECKING:
